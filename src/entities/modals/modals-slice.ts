import { createSlice } from "@reduxjs/toolkit";

const initialState = [
  { id: 1, title: "login", isOpen: false },
  { id: 2, title: "registration", isOpen: false },
];

const modalSlice = createSlice({
  name: "modals",
  initialState,
  redusers: {


    
  },
});
