export interface FilmData {
  id: number;
  tmdbRating?: number;
  releaseYear?: number;
  genres: string;
  runtime: number;
  title: string;
  plot: string;
  backdropUrl: string;
  posterUrl: string;
}
